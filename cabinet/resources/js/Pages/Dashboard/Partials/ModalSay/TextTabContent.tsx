const TextTabContent = ({ className }: { className: string }) => {
    return (
        <div className={className}>
            <p>TextTabContent</p>
        </div>
    );
};

export default TextTabContent;
