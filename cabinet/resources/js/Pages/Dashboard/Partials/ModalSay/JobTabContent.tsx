
const JobTabContent = ({ className }: { className: string }) => {
    return (
        <div className={ className }>
            <p>JobTabContent</p>
        </div>
      );
};

export default JobTabContent;
